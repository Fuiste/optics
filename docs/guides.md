# Quick start guide

Use this guide for practical examples and common workflows with `@fuiste/optics`:

- Install and import the optic constructor you need.
- Compose smaller optics to target nested values.
- Use `set` and `modify` for immutable updates.

This is a concise complement to the main README examples.
